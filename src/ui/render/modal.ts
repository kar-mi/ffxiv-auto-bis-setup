// Replaced by <CompareModal /> in CompareModal.tsx.
// These exports remain so existing callers (GearTab, AcquisitionTab, main.tsx) compile unchanged.
export { openCompareModal, closeModal } from "./CompareModal.tsx";
